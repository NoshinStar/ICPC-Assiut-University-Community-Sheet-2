#include <iostream>

using namespace std;

int main() {
    int a, b, max = 0;
    cin >> a >> b;
    for (int i = 1; i <= a || i <= b; i++)
        if ((a % i == 0) && (b % i == 0)) {
            if (i > max) max = i;
        }
    cout << max << endl;
    return 0;
}
