#include <iostream>

using namespace std;

int main() {
    string s;
    int n;
    cin >> s;
    cin >> n;

    for (int i = 0; i < n; i++) {
        int k;
        cin >> k;
        for (int j = 0; j < k; j++) {
            cout << s;
        }
        cout << endl;
    }

    return 0;
}
